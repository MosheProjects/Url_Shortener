// src/urlService.test.ts
// import Url from "../models/url";
// import { shortenUrl } from "../services/urlService";
// import { Url_Interfcae } from '../types/urlTypes';


// describe('URL Shortener Service', () => {
//   it('should generate a short URL and return a valid object', () => {
//     const originalUrl = 'http://example.com';
//     const result:Url_Interfcae = shortenUrl(originalUrl);

// expect(result.origUrl).toBe(originalUrl);
//     expect(result.shortUrl).toBeDefined();
//     expect(result.clicks).toBe(0);
//     expect(result.date).toBeInstanceOf(Date);
//   });
// });
